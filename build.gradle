defaultTasks 'jar'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: 'lib', include: '*.jar')
    compile group: 'org.controlsfx', name: 'controlsfx', version: '8.0.6'
}
